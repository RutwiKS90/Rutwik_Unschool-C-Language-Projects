#include <stdio.h>

int main()
{
    int num=0;
    printf("Enter a number:");
    scanf("%d",&num);
    if((num%2==0)
    {
        printf("The number %d is EVEN",num);
    }
    else
    {
        printf("The number %d is ODD",num);
    }
    return 0;
}
